{
    "name": "BetterAuth-StarterTemplate",
    "type": "module",
    "private": true,
    "scripts": {
        "build": "nuxt build",
        "dev": "nuxt dev",
        "generate": "nuxt generate",
        "preview": "pnpm run build && wrangler dev",
        "postinstall": "nuxt prepare",
        "deploy": "pnpm run build && wrangler deploy",
        "cf-typegen": "wrangler types"
    },
    "dependencies": {
        "@better-auth/prisma-adapter": "^1.6.29",
        "better-auth": "^1.6.29",
        "nuxt": "^4.5.2",
        "vue": "^3.5.41",
        "vue-router": "^5.2.0"
    },
    "devDependencies": {
        "@types/node": "^26.2.0",
        "nitro-cloudflare-dev": "^0.2.2",
        "wrangler": "^4.123.0"
    }
}
